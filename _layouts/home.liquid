<!doctype html>
<html lang="{{ site.lang }}">
  <head>
    {% include head.liquid %}
  </head>
  <body class="jia-home">
    {{ content }}
    <script src="{{ '/assets/js/portfolio-interactions.js' | relative_url }}" defer></script>
    {% include scripts.liquid %}
  </body>
</html>
